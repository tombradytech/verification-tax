# Buttondown API key. Set this in the Cloudflare Pages dashboard
# (Settings > Environment variables > Encrypt), NOT in a committed file.
# Only the Pages Function reads it; it never reaches the browser.
BUTTONDOWN_API_KEY=
